---
date: "2021-01-31T15:45:00"
title: "My first blog post!"
image: "https://cdn.britannica.com/25/172925-050-DC7E2298/black-cat-back.jpg"
description: "Read my first post on my blog built with Nextjs"
tags: "CSS, Animation"
---

# A Day in the Life of Whiskers

Meet **Whiskers**: a fluffy bundle of joy with the most captivating green eyes and a tail that wags like a tiny, soft metronome. Whiskers has a way of turning even the most mundane days into adventures.

![Whiskers in the garden](https://cdn.britannica.com/25/172925-050-DC7E2298/black-cat-back.jpg)

## Morning

The day begins with Whiskers, sunbathing by the window, soaking in the morning rays. It's a serene moment, punctuated only by the soft purrs and the occasional chirp at birds outside.

<Info>This is a code block with a title</Info>

```tsx showLineNumbers /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50)
const [name, setName] = useState("Taylor")
const [age, setAge] = useState(50)
const [name, setName] = useState("Taylor")
const [age, setAge] = useState(50)
const [name, setName] = useState("Taylor")
const [age, setAge] = useState(50)
const [name, setName] = useState("Taylor")
const [age, setAge] = useState(50)
const [name, setName] = useState("Taylor")
const [age, setAge] = useState(50)
const [name, setName] = useState("Taylor")
```

```tsx {1-2} {9-23} showLineNumbers{5} title="index.js"
function MyComponent() {
  const { refs, floatingStyles } = useFloating()

  return (
    <>
      <div ref={refs.setReference} />
      <div ref={refs.setFloating} style={floatingStyles} />
    </>
  )
}
function MyComponent() {
  const { refs, floatingStyles } = useFloating()

  return (
    <>
      <div ref={refs.setReference} />
      <div ref={refs.setFloating} style={floatingStyles} />
    </>
  )
}
function MyComponent() {
  const { refs, floatingStyles } = useFloating()

  return (
    <>
      <div ref={refs.setReference} />
      <div ref={refs.setFloating} style={floatingStyles} />
    </>
  )
}
function MyComponent() {
  const { refs, floatingStyles } = useFloating()

  return (
    <>
      <div ref={refs.setReference} />
      <div ref={refs.setFloating} style={floatingStyles} />
    </>
  )
}
function MyComponent() {
  const { refs, floatingStyles } = useFloating()

  return (
    <>
      <div ref={refs.setReference} />
      <div ref={refs.setFloating} style={floatingStyles} />
    </>
  )
}
function MyComponent() {
  const { refs, floatingStyles } = useFloating()

  return (
    <>
      <div ref={refs.setReference} />
      <div ref={refs.setFloating} style={floatingStyles} />
    </>
  )
}
function MyComponent() {
  const { refs, floatingStyles } = useFloating()

  return (
    <>
      <div ref={refs.setReference} />
      <div ref={refs.setFloating} style={floatingStyles} />
    </>
  )
}
```

```tsx {1-2} title="index.js" caption="Exibit. 114" showLineNumbers
import { useFloating } from "@floating-ui/react"

function MyComponent() {
  const { refs, floatingStyles } {= useFloating()= useFloating()= useFloating()= useFloating()= useFloating()= useFloating()= useFloating()}
  { refs, floatingStyles } {= useFloating()= useFloating()= useFloating()= useFloating()= useFloating()}

  return (
    <>
      <div ref={refs.setReference} />
      <div
        ref={refs.setFloating}
        style={floatingStyles}
        style={floatingStyles}
        style={floatingStyles}
        style={floatingStyles}
      />
    </>
  )
}
```
